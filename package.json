{
  "name": "@abeltib/monitor-shared-contracts",
  "version": "0.3.2",
  "description": "OpenAPI spec + generated TypeScript client/types for Monitor's Core API, consumed by monitor-internal/advertiser/agency/station so they never hand-drift from monitor-api.",
  "type": "module",
  "main": "dist/client.js",
  "types": "dist/client.d.ts",
  "files": [
    "dist",
    "openapi"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "scripts": {
    "codegen": "openapi-typescript openapi/v1.json -o src/schema.d.ts",
    "build": "npm run codegen && tsc -p tsconfig.json && node -e \"require('fs').copyFileSync('src/schema.d.ts','dist/schema.d.ts')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "openapi-fetch": "^0.13.0"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.0",
    "typescript": "^5.6.0"
  },
  "private": false,
  "license": "UNLICENSED"
}
